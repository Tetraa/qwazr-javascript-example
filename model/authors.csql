CREATE TABLE IF NOT EXISTS authors (
  user_name text,
  birth_year int,
  full_name text,
  password text,
  PRIMARY KEY ((user_name))
);
INSERT INTO authors (user_name, full_name, password) VALUES ('admin', 'Emmanuel', 'admin');
